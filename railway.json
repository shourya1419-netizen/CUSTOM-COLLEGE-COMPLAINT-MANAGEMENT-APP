{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "buildCommand": "",
    "startCommand": "cd backend && gunicorn config.wsgi:application --bind 0.0.0.0:$PORT",
    "preDeployCommand": "cd backend && python manage.py migrate && python manage.py create_admin"
  }
}
